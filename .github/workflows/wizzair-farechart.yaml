name: Wizzair Farechart Prices

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: "Start date (YYYY-MM-DD)"
        required: true
        default: "2026-05-01"
      end_date:
        description: "End date (YYYY-MM-DD)"
        required: true
        default: "2026-08-31"
      max_routes:
        description: "How many base routes from data/wizzair_polish_routes.csv to process (0 = all)"
        required: true
        default: "0"
      output_csv:
        description: "CSV path to save in repo"
        required: true
        default: "data/wizzair_farechart_prices_may_aug.csv"
      summary_json:
        description: "Summary JSON path to save in repo"
        required: true
        default: "data/wizzair_farechart_prices_may_aug_summary.json"
  schedule:
    - cron: "30 5 * * *"

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run farechart scrape
        env:
          START_DATE: ${{ github.event.inputs.start_date || '2026-05-01' }}
          END_DATE: ${{ github.event.inputs.end_date || '2026-08-31' }}
          MAX_ROUTES: ${{ github.event.inputs.max_routes || '0' }}
          OUTPUT_CSV: ${{ github.event.inputs.output_csv || 'data/wizzair_farechart_prices_may_aug.csv' }}
          SUMMARY_JSON: ${{ github.event.inputs.summary_json || 'data/wizzair_farechart_prices_may_aug_summary.json' }}
        run: |
          python scrape_wizzair_farechart_prices.py \
            --input-csv "data/wizzair_polish_routes.csv" \
            --output-csv "$OUTPUT_CSV" \
            --summary-json "$SUMMARY_JSON" \
            --start-date "$START_DATE" \
            --end-date "$END_DATE" \
            --max-routes "$MAX_ROUTES" \
            --include-return-routes \
            --day-interval 10 \
            --workers 8 \
            --retries 2 \
            --retry-delay-seconds 4 \
            --delay-seconds 0.25

      - name: Commit farechart outputs into repository
        env:
          OUTPUT_CSV: ${{ github.event.inputs.output_csv || 'data/wizzair_farechart_prices_may_aug.csv' }}
          SUMMARY_JSON: ${{ github.event.inputs.summary_json || 'data/wizzair_farechart_prices_may_aug_summary.json' }}
        run: |
          if [ -f "$OUTPUT_CSV" ]; then
            git add "$OUTPUT_CSV"
            if [ -f "$SUMMARY_JSON" ]; then
              git add "$SUMMARY_JSON"
            fi
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "update wizzair farechart prices" || echo "No farechart changes to commit"
            git push
          else
            echo "CSV not found: $OUTPUT_CSV"
            exit 1
          fi

      - name: Upload farechart artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wizzair-farechart-output
          path: |
            ${{ github.event.inputs.output_csv || 'data/wizzair_farechart_prices_may_aug.csv' }}
            ${{ github.event.inputs.summary_json || 'data/wizzair_farechart_prices_may_aug_summary.json' }}
          if-no-files-found: error
